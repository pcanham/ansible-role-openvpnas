# Set this to false if you do not wish to configure LDAP within openvpnas
enable_ldap_config: true

# Set this to false if you do not wish to configure letsencrypt integration with openvpnas
enable_letencrypt_config: true